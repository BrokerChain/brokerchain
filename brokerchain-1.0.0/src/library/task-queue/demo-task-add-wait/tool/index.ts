// auto generated by dev/system

export function make_input(cb?: (o: { queue_id: string; mode: "resolve" | "15s-resolve" | "reject" | "15s-reject" | "infinite" }) => void): {
    queue_id: string;
    mode: "resolve" | "15s-resolve" | "reject" | "15s-reject" | "infinite";
} {
    const item: { queue_id: string; mode: "resolve" | "15s-resolve" | "reject" | "15s-reject" | "infinite" } = { queue_id: "", mode: "resolve" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
