// auto generated by dev/system

export function make_input(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { list: { id: string; task_list: { id: string; status: "pending" | "running" }[] }[] }) => void): {
    list: { id: string; task_list: { id: string; status: "pending" | "running" }[] }[];
} {
    const item: { list: { id: string; task_list: { id: string; status: "pending" | "running" }[] }[] } = { list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
