// auto generated by dev/system

export function make_input(cb?: (o: { queue_id: string }) => void): { queue_id: string } {
    const item: { queue_id: string } = { queue_id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { task_id: string }) => void): { task_id: string } {
    const item: { task_id: string } = { task_id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
