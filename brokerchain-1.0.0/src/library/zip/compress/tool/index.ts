// auto generated by dev/system

export function make_input(cb?: (o: { from_path: string; to_file: string; without_parent_dir?: boolean }) => void): {
    from_path: string;
    to_file: string;
    without_parent_dir?: boolean;
} {
    const item: { from_path: string; to_file: string; without_parent_dir?: boolean } = { from_path: "", to_file: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
