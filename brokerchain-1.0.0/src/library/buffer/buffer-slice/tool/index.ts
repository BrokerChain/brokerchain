// auto generated by dev/system

export function make_input(cb?: (o: { hex: string; byte_start: number; byte_end: number }) => void): { hex: string; byte_start: number; byte_end: number } {
    const item: { hex: string; byte_start: number; byte_end: number } = { hex: "", byte_start: 0, byte_end: 0 };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_empty(cb?: (o: { sliced_hex: string }) => void): { sliced_hex: string } {
    const item: { sliced_hex: string } = { sliced_hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { sliced_hex: string }) => void): { sliced_hex: string } {
    const item: { sliced_hex: string } = { sliced_hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
