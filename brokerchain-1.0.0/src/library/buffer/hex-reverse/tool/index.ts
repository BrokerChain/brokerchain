// auto generated by dev/system

export function make_input(cb?: (o: { hex: string }) => void): { hex: string } {
    const item: { hex: string } = { hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { hex_reversed: string }) => void): { hex_reversed: string } {
    const item: { hex_reversed: string } = { hex_reversed: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
