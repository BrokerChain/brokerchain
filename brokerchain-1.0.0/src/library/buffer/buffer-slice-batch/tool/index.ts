// auto generated by dev/system

export function make_input(cb?: (o: { hex: string; byte_start: number; byte_length_list: number[] }) => void): {
    hex: string;
    byte_start: number;
    byte_length_list: number[];
} {
    const item: { hex: string; byte_start: number; byte_length_list: number[] } = { hex: "", byte_start: 0, byte_length_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_empty(cb?: (o: { sliced_hex_list: string[] }) => void): { sliced_hex_list: string[] } {
    const item: { sliced_hex_list: string[] } = { sliced_hex_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { sliced_hex_list: string[] }) => void): { sliced_hex_list: string[] } {
    const item: { sliced_hex_list: string[] } = { sliced_hex_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
