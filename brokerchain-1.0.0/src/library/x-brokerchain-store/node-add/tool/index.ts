// auto generated by dev/system

export function make_input(cb?: (o: { node: { fake?: boolean; address: string; port: number } }) => void): {
    node: { fake?: boolean; address: string; port: number };
} {
    const item: { node: { fake?: boolean; address: string; port: number } } = { node: { address: "", port: 0 } };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { id: string }) => void): { id: string } {
    const item: { id: string } = { id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
