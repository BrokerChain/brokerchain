// auto generated by dev/system

export function make_input(cb?: (o: { peer: { id: string; fake?: boolean; address: string; port: number } }) => void): {
    peer: { id: string; fake?: boolean; address: string; port: number };
} {
    const item: { peer: { id: string; fake?: boolean; address: string; port: number } } = { peer: { id: "", address: "", port: 0 } };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
