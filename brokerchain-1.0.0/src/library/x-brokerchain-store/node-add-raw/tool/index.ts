// auto generated by dev/system

export function make_input(cb?: (o: { node: { fake?: boolean; create_time: string; update_time: string; address: string; port: number } }) => void): {
    node: { fake?: boolean; create_time: string; update_time: string; address: string; port: number };
} {
    const item: { node: { fake?: boolean; create_time: string; update_time: string; address: string; port: number } } = {
        node: { create_time: "", update_time: "", address: "", port: 0 }
    };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { id: string }) => void): { id: string } {
    const item: { id: string } = { id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
