// auto generated by dev/system

export function make_input(cb?: (o: { peer: { fake?: boolean; address: string; port: number } }) => void): {
    peer: { fake?: boolean; address: string; port: number };
} {
    const item: { peer: { fake?: boolean; address: string; port: number } } = { peer: { address: "", port: 0 } };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { id: string }) => void): { id: string } {
    const item: { id: string } = { id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
