// auto generated by dev/system

export function make_input(cb?: (o: { node: { id: string; fake?: boolean; address: string; port: number } }) => void): {
    node: { id: string; fake?: boolean; address: string; port: number };
} {
    const item: { node: { id: string; fake?: boolean; address: string; port: number } } = { node: { id: "", address: "", port: 0 } };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
