// auto generated by dev/system

export function make_input(cb?: (o: { path: string; mode: number }) => void): { path: string; mode: number } {
    const item: { path: string; mode: number } = { path: "", mode: 0 };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
