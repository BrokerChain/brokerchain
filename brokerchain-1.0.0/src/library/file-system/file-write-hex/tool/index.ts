// auto generated by dev/system

export function make_input(cb?: (o: { name: string; hex: string }) => void): { name: string; hex: string } {
    const item: { name: string; hex: string } = { name: "", hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
