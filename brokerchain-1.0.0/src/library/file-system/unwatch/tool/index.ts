// auto generated by dev/system

export function make_input(cb?: (o: { watcher_id: string }) => void): { watcher_id: string } {
    const item: { watcher_id: string } = { watcher_id: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
