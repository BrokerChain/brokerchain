// auto generated by dev/system

export function make_input(cb?: (o: { name: string }) => void): { name: string } {
    const item: { name: string } = { name: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { text: string }) => void): { text: string } {
    const item: { text: string } = { text: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
