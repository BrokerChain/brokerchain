// auto generated by dev/system

export function make_input(cb?: (o: { dir: string; recursive: boolean }) => void): { dir: string; recursive: boolean } {
    const item: { dir: string; recursive: boolean } = { dir: "", recursive: false };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_empty(cb?: (o: { file_list: { name: string; fullname: string }[] }) => void): { file_list: { name: string; fullname: string }[] } {
    const item: { file_list: { name: string; fullname: string }[] } = { file_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { file_list: { name: string; fullname: string }[] }) => void): { file_list: { name: string; fullname: string }[] } {
    const item: { file_list: { name: string; fullname: string }[] } = { file_list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
