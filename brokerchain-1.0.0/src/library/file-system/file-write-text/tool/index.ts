// auto generated by dev/system

export function make_input(cb?: (o: { name: string; text: string }) => void): { name: string; text: string } {
    const item: { name: string; text: string } = { name: "", text: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
