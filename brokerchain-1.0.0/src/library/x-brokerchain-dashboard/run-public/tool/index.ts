// auto generated by dev/system

export function make_input(cb?: (o: { build: boolean }) => void): { build: boolean } {
    const item: { build: boolean } = { build: false };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
