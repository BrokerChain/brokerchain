// initialized by dev/system

import { Footer } from "../index.js";
import { value_fun, ValueIO, log } from "../../export.local.js";

export function will_unmount(self: Footer) {
    const { value, io } = self.props;
    // TODO
}
