// initialized by dev/system

import { Mining } from "../index.js";
import { value_fun, ValueIO, log } from "../../export.local.js";

export function will_unmount(self: Mining) {
    const { value, io } = self.props.vio;
    // TODO
}
