// auto generated by dev/system

export function make_input(cb?: (o: { build: boolean; host: string; http_port: number; https_port: number }) => void): {
    build: boolean;
    host: string;
    http_port: number;
    https_port: number;
} {
    const item: { build: boolean; host: string; http_port: number; https_port: number } = { build: false, host: "", http_port: 0, https_port: 0 };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}
