// auto generated by dev/system

import * as React from "react";
import { render } from "./render.js";
import { RootProps, RootState, init_state } from "./type.js";
import { did_mount } from "./lifecycle/did_mount.js";
import { will_unmount } from "./lifecycle/will_unmount.js";
import { Extend } from "./extend.js";

export class Root extends React.PureComponent<RootProps, RootState> {
    extend: Extend;

    constructor(props: RootProps) {
        super(props);
        this.state = init_state();
        this.extend = new Extend(this);
    }

    componentDidMount() {
        did_mount(this);
    }

    componentWillUnmount() {
        will_unmount(this);
    }

    render() {
        return render(this);
    }
}
