// auto generated by dev/system

import * as React from "react";
import { FooterProps } from "./type.js";
import { Footer } from "./index.js";
import { Value, ValueIO } from "../type/index.js";
import { empty } from "../value/index.js";

interface Props extends Omit<FooterProps, "vio"> {
    init?: (value: Value) => Value;
}

interface State {
    value: Value;
}

export class FooterWrapper extends React.Component<Props, State> {
    latest_value: Value;
    io: ValueIO;

    constructor(props: Props) {
        super(props);
        this.latest_value = props.init ? props.init(empty()) : empty();
        this.state = {
            value: this.latest_value
        };
        this.io = new ValueIO({
            read: () => {
                return this.latest_value;
            },
            write: (v, silent) => {
                this.latest_value = v;
                if (!silent) {
                    this.setState({
                        value: v
                    });
                }
                // console.log(`IndexFooterWrapper.io.write()`, JSON.stringify(v.toJS(), null, 4));
            }
        });
    }

    render() {
        // console.log(`IndexFooterWrapper.render()`);
        return <Footer {...this.props} vio={{ value: this.state.value, io: this.io }} />;
    }
}
