// auto generated by dev/system

import { Logger } from "../../../myutils/logger.js";
import { _start } from "./_start.js";
import yargs from "yargs";
import { hideBin } from "yargs/helpers";
import { extend } from "./extend.js";

const argv = await yargs(hideBin(process.argv)).string("host").number("http_port").number("https_port").parse();

const log = new Logger("pty.server");

_start(log, {
    host: argv.host || "127.0.0.1",
    http_port: argv.http_port || 3000,
    https_port: argv.https_port || 8433,
    setup: (exp_app) => {
        extend(log, exp_app, "/library/pty");
    }
});
