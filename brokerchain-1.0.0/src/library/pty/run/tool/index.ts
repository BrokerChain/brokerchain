// auto generated by dev/system

export function make_input(cb?: (o: { file: string; args: string[]; text?: string; cwd?: string }) => void): {
    file: string;
    args: string[];
    text?: string;
    cwd?: string;
} {
    const item: { file: string; args: string[]; text?: string; cwd?: string } = { file: "", args: [] };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { exit_code: number }) => void): { exit_code: number } {
    const item: { exit_code: number } = { exit_code: 0 };
    if (cb) {
        cb(item);
    }
    return item;
}
