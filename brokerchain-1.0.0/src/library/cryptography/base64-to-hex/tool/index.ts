// auto generated by dev/system

export function make_input(cb?: (o: { base64: string }) => void): { base64: string } {
    const item: { base64: string } = { base64: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { hex: string }) => void): { hex: string } {
    const item: { hex: string } = { hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
