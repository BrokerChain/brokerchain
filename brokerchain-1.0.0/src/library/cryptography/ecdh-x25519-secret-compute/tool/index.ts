// auto generated by dev/system

export function make_input(cb?: (o: { my_private_key_hex: string; target_public_key_hex: string }) => void): {
    my_private_key_hex: string;
    target_public_key_hex: string;
} {
    const item: { my_private_key_hex: string; target_public_key_hex: string } = { my_private_key_hex: "", target_public_key_hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { secret: { hex: string; base64: string } }) => void): { secret: { hex: string; base64: string } } {
    const item: { secret: { hex: string; base64: string } } = { secret: { hex: "", base64: "" } };
    if (cb) {
        cb(item);
    }
    return item;
}
