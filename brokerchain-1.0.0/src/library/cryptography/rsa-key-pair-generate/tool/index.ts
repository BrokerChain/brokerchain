// auto generated by dev/system

export function make_input(cb?: (o: { bits?: number }) => void): { bits?: number } {
    const item: { bits?: number } = {};
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { pem_private_key: string; pem_public_key: string }) => void): { pem_private_key: string; pem_public_key: string } {
    const item: { pem_private_key: string; pem_public_key: string } = { pem_private_key: "", pem_public_key: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
