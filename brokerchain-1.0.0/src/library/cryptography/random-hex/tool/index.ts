// auto generated by dev/system

export function make_input(cb?: (o: { bytes: number }) => void): { bytes: number } {
    const item: { bytes: number } = { bytes: 0 };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { hex: string }) => void): { hex: string } {
    const item: { hex: string } = { hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
