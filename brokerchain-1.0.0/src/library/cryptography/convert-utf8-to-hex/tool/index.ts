// auto generated by dev/system

export function make_input(cb?: (o: { utf8: string }) => void): { utf8: string } {
    const item: { utf8: string } = { utf8: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { hex: string }) => void): { hex: string } {
    const item: { hex: string } = { hex: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
