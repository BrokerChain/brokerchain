// auto generated by dev/system

export function make_input(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { private_key: { pem: string }; public_key: { pem: string } }) => void): {
    private_key: { pem: string };
    public_key: { pem: string };
} {
    const item: { private_key: { pem: string }; public_key: { pem: string } } = { private_key: { pem: "" }, public_key: { pem: "" } };
    if (cb) {
        cb(item);
    }
    return item;
}
