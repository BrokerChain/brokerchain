// auto generated by dev/system

export function make_input(cb?: (o: {}) => void): {} {
    const item: {} = {};
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { private_key: { hex: string; base64: string }; public_key: { hex: string; base64: string } }) => void): {
    private_key: { hex: string; base64: string };
    public_key: { hex: string; base64: string };
} {
    const item: { private_key: { hex: string; base64: string }; public_key: { hex: string; base64: string } } = {
        private_key: { hex: "", base64: "" },
        public_key: { hex: "", base64: "" }
    };
    if (cb) {
        cb(item);
    }
    return item;
}
