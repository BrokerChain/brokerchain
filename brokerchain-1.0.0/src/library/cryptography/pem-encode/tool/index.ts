// auto generated by dev/system

export function make_input(cb?: (o: { type: string; content: string }) => void): { type: string; content: string } {
    const item: { type: string; content: string } = { type: "", content: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { result: string }) => void): { result: string } {
    const item: { result: string } = { result: "" };
    if (cb) {
        cb(item);
    }
    return item;
}
