// auto generated by dev/system

export function make_input(cb?: (o: { text: string }) => void): { text: string } {
    const item: { text: string } = { text: "" };
    if (cb) {
        cb(item);
    }
    return item;
}

export function make_output_ok(cb?: (o: { list: { type: string; content: string }[] }) => void): { list: { type: string; content: string }[] } {
    const item: { list: { type: string; content: string }[] } = { list: [] };
    if (cb) {
        cb(item);
    }
    return item;
}
